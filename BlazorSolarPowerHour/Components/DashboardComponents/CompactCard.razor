<div class="k-d-flex k-bg-surface-alt k-align-items-center k-overflow-x-auto k-gap-2">
    <span class="symbol">@Symbol</span>
    <div class="k-d-flex k-flex-col">
        <span class="k-font-size-xl">@Value @Unit</span>
        @if (Text is not null)
        {
            <span class="k-font-size-sm k-color-subtle">@Text</span>
        }
    </div>
</div>

@code {
    [Parameter]
    public string Value { get; set; } = string.Empty;

    [Parameter]
    public string? Text { get; set; }

    [Parameter]
    public string Symbol { get; set; } = string.Empty;

    [Parameter]
    public string Unit { get; set; } = string.Empty;
}
